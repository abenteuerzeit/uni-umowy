<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Generator Dokumentów ASP</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous" />

    <style type="text/css">
        body {
            background-color: #f5f5f5;
        }

        .container {
            max-width: 960px;
            display: flex;
            flex-direction: column;
            align-items: stretch;
            justify-content: center;
            margin-top: 5vh;
        }

        h1, h2 {
            font-weight: 300;
            margin: 1vh 0 0 0;
            padding: 1vh;
        }

        ul, li {
            list-style: none;
            margin: 0;
            padding: 0;
        }

        ul, li, a {
            text-decoration: underline;
            color: #333;
        }

        p {
            margin-top: 0;
            margin-bottom: 1rem;
            padding: auto;
        }



        @media (min-width: 768px) {
            .bd-placeholder-img-lg {
                font-size: 3.5rem;
            }
        }
    </style>
</head>

<body class="container">

    {{{body}}}

</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN"
    crossorigin="anonymous"></script>
<script>
    const input = document.getElementById('input');
button.addEventListener('click', ()=>{
    const ajax = new XMLHttpRequest();
    const val = input.value;
    if (val === '') {
        document.getElementById('amount-in-words').innerHTML = 'Please enter a number';
        return;
    }
    ajax.open('GET', `/slownie?payout=${val}`);
    ajax.onload = function() {
        if (this.status === 200) {
            var data = JSON.parse(this.responseText);
            document.getElementById('amount-in-words').innerHTML = data.payout;
        }
        if (this.status === 400) {
            document.getElementById('amount-in-words').innerHTML = '400 - Bad Request';
        }
    }
    ajax.send();
});
</script>
</html>